import React from 'react';
import '../styles/styles.css';


function Specials() {
  // Fetch specials data and loop over them
//   const specialsData = []; // Replace with actual data

  return (
    <div>
      <h2 className="special-offer">Specials</h2>
      {/* Loop over specialsData and render each special */}
    </div>
  );
}

export default Specials;